function setup(){
    createCanvas(400,400);
}

function draw(){
    let counter=0;
    background(117, 172, 255);
    makeCircles(200, 200, 0);
}

function makeCircles(x,y, counter){
//issue w recursion
}